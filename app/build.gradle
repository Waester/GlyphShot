apply plugin: 'com.android.application'

android {
    compileSdkVersion 11

    defaultConfig {
        applicationId "com.github.glyphshot"
        minSdkVersion 11
        targetSdkVersion 11
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}
